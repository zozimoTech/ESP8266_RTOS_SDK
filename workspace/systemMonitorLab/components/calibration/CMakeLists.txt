# Componente reservado para los parametros de calibracion.

# Cuando se agregue codigo fuente, descomentar y completar:
# set(COMPONENT_SRCS "calibration.c")
# set(COMPONENT_ADD_INCLUDEDIRS ".")

register_component()
